#!/bin/bash
set -euo pipefail

PACKAGES=(
  build-essential
  file
  g++
  gawk
  gettext
  git
  libncurses5-dev
  libssl-dev
  libxml-parser-perl
  man-db
  python
  python3
  rsync
  subversion
  unzip
  wget
  zip
  zlib1g-dev
)

apt-get install -y "${PACKAGES[@]}"
